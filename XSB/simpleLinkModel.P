% BEGIN IMPORTS
:- import  isEncryptedButFor/2 from simpleLinkModelInstance.
:- import  attacker/1 from securityInstance.
% END IMPORTS
% BEGIN EXPORTS
:- export  exposesPhsicallyAccessibleDataTo/3.
% END IMPORTS

justify_pred(exposesPhsicallyAccessibleDataTo(_,_,_)).

exposesPhsicallyAccessibleDataTo(Link,Attacker,DataSet) :-
        attacker(Attacker),
        encryptsExcept(Link, Ds).
